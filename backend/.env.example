# Database connection URL. For local development, the application can use its existing SQLite default if this variable is not provided.
DATABASE_URL=

# URL of the frontend allowed to access the backend through CORS.
FRONTEND_URL=https://your-frontend-url.example.com

# Application environment such as development or production.
ENVIRONMENT=development
